transformers==4.43.3
accelerate==0.33.0
tokenizers>=0.15
protobuf>=3.20,<5
firebase-admin==6.5.0
google-cloud-firestore==2.16.0
google-auth==2.32.0
tqdm

# 실행 docker compose up -d summarizer
# 실시간 로그 docker compose logs -f summarizer
# 종료 docker compose stop summarizer
